<h1>My Stations</h1>

<p><%= link_to 'Add a Station', new_station_path, class: 'link' %></p>

<ul>
  <% @stations.each do |station| %>
    <li>
      <%= link_to station.name, station_path(station) %>
    </li>
  <% end %>
</ul>